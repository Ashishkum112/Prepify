# Optional locally. Required only for Google Calendar/Gmail integration.
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"

# Default recipient shown in Settings. Leave blank for local-only tracking.
DEFAULT_REMINDER_EMAIL=""

# Hosting providers normally inject PORT automatically.
PORT=3000
